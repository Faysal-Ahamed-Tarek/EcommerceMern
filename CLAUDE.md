# Task Prompt

Update the ecommerce UI copy so the home page section currently labeled "Featured Products" shows as "For you" instead. Also rename the admin product checkbox label from "Featured product" to "For you". Keep the existing featured-product behavior unchanged; only adjust the visible text and matching loading-state copy.


2. add a phone number with icon, in header right. add a option in admin pages header, to input phone number.