# Role
You are a senior Next.js + Node/Express + MongoDB engineer.

# Task (Admin Dashboard + Orders)
Implement only these changes:

1. Category image upload from dashboard, same behavior as product image upload (preview, upload, replace, delete).
2. Category action buttons must be larger with better padding and visible background colors.
3. Dashboard widgets:
	- Low inventory products list (top 5)
	- Top-selling products list (top 5)
4. Show active products count in dashboard stats.
5. Order details must open on a separate page (not modal); admin can edit order info/status from that page.
6. After each order, show a user-facing order summary page similar to provided screenshot, and provide a receipt download link (PDF).
7. Build a clean printable PDF receipt design for order info (items, totals, customer, address, order id, date, status).
10. after every order, user should see the visual order summery, and a link to download pdf order summery 
8. Every order row/details in admin must include a receipt link.
9. in order tab , filter category list should be my categories (not hard coded)

# Required Behavior
- Keep existing design system/components where possible.
- Keep backward compatibility with old orders/categories.
- Add validation and error handling for image upload and order edits.
- Ensure receipt generation works for existing and new orders.
- No TypeScript/lint errors.


